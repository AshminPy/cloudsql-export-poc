SANITIZED POC EVIDENCE -- no real identifiers appear in this output; included as-is.

Test: run seed/validate.sql against the restored database. This is the strengthened
version of the test, per the requirement to explicitly verify the stored procedure
with a real CALL and a before/after data check, not just presence of its definition.

--- tables present ---
audit_log, customer_order_totals, customers, order_items, orders   (5/5)

--- row counts ---
customers: 10 | orders: 12 | order_items: 14 | audit_log: 24
(exact match to the seed dataset, including audit rows generated by the trigger)

--- sample record check (customer #1) ---
matches the seeded row exactly

--- indexes present ---
7/7 (2 explicit + primary key / unique constraints)

--- view present + queryable ---
customer_order_totals: present, returns correct computed order counts and totals

--- function present? (recalculate_order_total) ---
present

--- trigger function present? (log_order_change) ---
present

--- trigger present on orders table? ---
trg_orders_audit: present, correctly attached to orders

--- stored procedure present, and classified as a real PROCEDURE (prokind=p)? ---
mark_order_shipped | p        <- confirms Postgres itself classifies this as a
                                  genuine PROCEDURE object, not a function

--- order #3 status BEFORE calling the procedure ---
order_id 3 | status: pending

--- CALL mark_order_shipped(3) ---
CALL

--- order #3 status AFTER calling the procedure ---
order_id 3 | status: shipped   <- the procedure actually executed and changed real
                                  data, not just survived as inert text in the dump

RESULT: every object type tested (tables, rows, indexes, view, functions, trigger,
and a genuine CALL-able stored procedure) survived the export/download/restore
cycle intact and working. This directly contradicts Google's own documentation,
which states SQL export "does not contain triggers or stored procedures." See the
README's "pg_dump compatibility findings" section for the full analysis and why this
result must be re-verified against any other database's real schema before relying
on it -- it is evidence for this test case, not a universal guarantee.
